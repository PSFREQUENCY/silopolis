anthropic>=0.40.0
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
httpx>=0.28.0
pydantic>=2.0.0
web3>=7.0.0
eth-account>=0.13.0
python-dotenv>=1.0.0
# Uniswap / EVM
uniswap-universal-router-decoder>=0.7.0
eth-abi>=5.0.0
# Persistence
# (sqlite3 is stdlib — no extra install needed)
